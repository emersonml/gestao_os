version: "3.9"
services:
  54nginx:
    image: nginx:alpine
    container_name: mapos-54-nginx
    hostname: 54nginx
    volumes:
      - "./etc/nginx/default.conf:/etc/nginx/conf.d/default.conf"
      - "../:/var/www/html"
      - "./etc/nginx/default.template.conf:/etc/nginx/conf.d/default.template"
    networks:
      nginx_network:
        ipv4_address: 10.0.2.54
      54network:
        ipv4_address: 10.0.54.2
    ports:
      - "${NGINX_PORT}:${NGINX_PORT}"
    env_file:
      - ".env"
    command: /bin/sh -c "envsubst '$$NGINX_HOST $$NGINX_PORT' < /etc/nginx/conf.d/default.template > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
    restart: always
    depends_on:
      - 54php-fpm
      - 54mysql

  54php-fpm:
    container_name: mapos-54-php-fpm
    hostname: 54php-fpm
    build:
      context: ./etc/php
      dockerfile: Dockerfile
    restart: always
    volumes:
      - "./etc/php/php.ini:/usr/local/etc/php/conf.d/php.ini"
      - "./etc/php/uploads.ini:/usr/local/etc/php/conf.d/uploads.ini"
      - "../:/var/www/html"
    networks:
      54network:
        ipv4_address: 10.0.54.4

  54composer:
    container_name: mapos-esoft-composer
    hostname: 54composer
    build:
      context: ./etc/composer
      dockerfile: Dockerfile
    volumes:
      - "../:/app"
    networks:
      54network:
        ipv4_address: 10.0.54.5
    command: install --ignore-platform-reqs --no-scripts



  54mysql:
    image: mysql:${MYSQL_MAPOS_VERSION}
    container_name: mapos-esoft-mysql
    hostname: 54mysql
    restart: always
    env_file:
      - ".env"
    environment:
      - MYSQL_DATABASE=${MYSQL_MAPOS_DATABASE}
      - MYSQL_ROOT_PASSWORD=${MYSQL_MAPOS_ROOT_PASSWORD}
      - MYSQL_USER=${MYSQL_MAPOS_USER}
      - MYSQL_PASSWORD=${MYSQL_MAPOS_PASSWORD}
    networks:
      54network:
       ipv4_address: 10.0.54.3
    ports:
      - "${MYSQL_MAPOS_PORT}:3306"
    volumes:
      - "./data/db/mysql:/var/lib/mysql"



networks:
  54network:
    driver: bridge
    ipam:
      config:
        - subnet: 10.0.54.0/24
          gateway: 10.0.54.1
  nginx_network:
    external: true